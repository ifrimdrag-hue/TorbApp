docs_input/
data/
scripts/
.venv/
.torb.pid

# Logs (runtime)
logs/
*.log

# Python
test_results.txt
__pycache__/
*.py[cod]
*$py.class
*.so
.pytest_cache/
.ruff_cache/

# Claude Code / AI session artifacts — ignore session state, keep shared skills
.claude/settings.json
.claude/settings.local.json
.claude/__store/
docs/specs/
extra/

# Manuals — Typst sources/images in subdirs are not versioned; only the
# compiled PDFs at docs/manuals/*.pdf are
docs/manuals/**
!docs/manuals/*.pdf

# Windows crash dumps
*.stackdump

# Secrets / local env (keep .env.example in repo if you add one)
.env
.env.local
.env.*.local

# Server secrets — migrated 2026-06-17 to a KeePassXC vault (torb-secrets.kdbx, kept OUTSIDE the repo).
# Rules retained as safety nets: NEVER commit the old plaintext companion or any key vault.
docs/manuals/server/secrets.local.md
*.kdbx

# macOS
.DS_Store

# Windows system files
desktop.ini
Thumbs.db
